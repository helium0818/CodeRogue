# CodeRogue Rogue 化设计笔记

> 目标：让“写对固件”成为每一局远征里必须重新思考的核心决策；在保教学的前提下加大难度与深度。
> 本文档是设计与实测记录，不替代 `GAME_DESIGN.md` 的长期愿景；可执行进度会拆进 `TASKS.md`。

## 1. 现状问题（2026-09-04 实测）

### 1.1 一份固件可以通吃大部分战场

用自动扫场脚本对全部战斗池（combat / elite / boss × seed 1,2,42,2026）做“真实玩家”测试：

- 一个“贴墙右转 + 敌人相邻就 attack”的通用固件，只要在死代码里凑满词法约束，就能通过几乎所有战斗节点。
- 一个“贴墙右转 + 敌人 2 格内 ranged_attack”的通用固件同样能通过 combat / elite。
- 词法约束（require/forbid）只检查“源码里有没有这些字符串”，玩家可以把 `if(false){ ranged_attack(); }` 当成“镀金装饰”绕过意图，完全没有激励按敌人类型改写策略。

结论：远征仍不够“代码即胜负”，难度主要来自体力活而不是算法决策。

### 1.2 “载入参考解”会直接跳过挑战

编辑器里始终有“载入参考解”按钮，等于每个战场都提供一份正确答案。新玩家可以用它一键通关，资深玩家也不会得到任何惩罚或限制。

### 1.3 失败的代价太低

机体损毁或运行错误后，节点不推进、船体不扣血，玩家可以无限重试。作为 rogue，失败缺少真实代价，也就没有“要不要硬闯高风险节点”的张力。

### 1.4 元进度 UI 是坏的

设置面板渲染了 `metaUpgradesCatalog` 与 `purchaseUpgrade`，但源码里并没有这两个定义：打开设置不会展示机体升级，点击也会在运行时失败。跨局资源（credits/bestGrade）只能看数字，不能变成选择。

### 1.5 战利品与战斗逻辑仍是“先打赢再给数值”

普通战斗 S/A/B/C 已改为直接授予模块，方向是对的；但模块池仍然偏数值（伤害/能量/减伤），缺少能改变“下一场该怎么写代码”的模块。

### 1.6 非战斗节点仍是两段菜单

事件/商店的“行动 → 战利品”都走独立弹窗，节奏偏慢；和“写代码”这个核心乐趣关系较弱。

## 2. 从同类 rogue / 解谜游戏里学什么

### 2.1 Into the Breach

- 敌人行动完全透明（下一手可见），玩家先读局面再操作；难度来自“多个问题互相冲突”，不是随机偷袭。
- 学到：每个战场都应先给出“敌人会怎么动、威胁距离、反制传感器”，再让固件去解决；难度不是堆数值，而是让一个反制同时产生另一个风险。

### 2.2 FTL

- 有限资源必须分配给多个同时发生的问题，玩家每一站都要决定“这次冒多大险”。
- 学到：远征需要让玩家在“保留船体、压时间、多拿模块”之间做取舍；失败必须在船体/资源上留下痕迹。

### 2.3 Slay the Spire

- 构筑不是数值越高越好，而是让不同卡组面对不同 BOSS 产生不同解法。
- 学到：S/A/B/C 奖励应该朝“行为差异”分化——有的奖励让玩家可以远程压制，有的让玩家可以近身硬吃，有的让玩家可以看穿敌人弱点。

### 2.4 Zachtronics / Shenzhen I/O

- 玩家对同一问题有大量不同解法；真正的深度来自“评分”和“自我优化”，而不是“有没有答对”。
- 学到：固件评级已经往这个方向走；下一步要让评级理由可见（快多少、伤多少、能耗多少、读了多少传感器），并让高评级与低评级在构筑上产生不同分支。

### 2.5 乐趣函数小结

让 CodeRogue 好玩的四个来源：

1. **读懂敌人**：敌人先行动原则可见，玩家必须按“这一个”敌人重写判断顺序。
2. **写对优先级**：感知/移动/攻击/防御的顺序不同，结果完全不同；菜单永远不该替代这段思考。
3. **承担代价**：船体、能耗、时间、模块上限都是资源，硬闯要有后果。
4. **构筑塑造解法**：奖励应改变“下一场用什么传感器/动作”，而不只是把数字变大。

## 3. 本轮目标（先完成这些，再实测迭代）

### G1：让战场按敌人类型强制分叉

- 给普通战斗加入“只能远程 / 只能近战 / 必须自定函数”等禁手约束，并且约束不再能被死代码镀金。
- 至少保证：同一份“通用固件”无法同时通过近战关与远程关；全场景池自动扫场覆盖这点。
- 验收：新增测试断言 no single firmware 能通过全部战斗池。

### G2：给“参考解”加上真实门槛

- 正常游玩不再一键载入完整参考解；参考解仅用于失败后的引导或自动演示。
- 失败面板保留“保留代码重试”和“载入战场草稿”，让玩家自己把草稿改成答案。
- 验收：编辑器在初次进入战场时不显示“载入参考解”。

### G3：让失败留下代价

- 机体损毁（非编译/运行错误）时节点不推进，但船体 -1；船体归零则该局提前结束为“失联”，不结算胜利资源。
- 验收：新增测试覆盖失败扣船体与船体归零提前结束。

### G4：修复并做实元进度

- 补上 `metaUpgradesCatalog` 与 `purchaseUpgrade`，让 credits 可购买可持久化的机体升级（船体/能量/远程校准至少各一个）。
- 验收：购买后升级持久化并真实作用于远征 modifiers。

### G5：提高战利品对写代码的塑造力（后续迭代）

- 让至少一个模块改变可用动作/传感器语义（例如远程校准、护盾自动展开），并在 Trace 里可见。
- 放在 G1-G4 稳定后再做，避免一次改动范围过大。

## 4. 实测记录

（每次以“真实玩家”流程跑通后，把结果追加到这里。）
### 第一轮落地记录 / 2026-09-04

自动扫场确认：

- 旧的通用“贴墙 + 近战”固件确实能通过几乎全部 combat / elite / boss。
- 词法约束可被死代码镀金；且 `forbid: ['attack()']` 会误伤 `ranged_attack()`（子串匹配 bug）。

本轮改动：

- 普通战斗/精英全部禁止真实 `attack()`，BOSS 禁止真实 `ranged_attack()`；约束匹配改为函数名边界，不再用子串。
- “载入参考解”改为失败后解锁，正常首战只能看到“战场草稿”。
- 机体损毁会扣除 1 船体；船体归零触发本局失联结束。
- 修复设置面板缺失的 `metaUpgradesCatalog` / `purchaseUpgrade`，并让 credits 可购买船体/能量/远程校准。

验证：

- `npm test`：62 个测试通过（含新 anti-generic 扫场与失败代价覆盖）。
- `npm run build`：通过。
### 第二轮落地记录 / 2026-09-04

- 新增 `scaleExpeditionBattle`：越深入路线，敌人 HP 越高；后期节点还会提升移动/攻击频率。
- App 进入战场与胜利结算使用同一套 scaling，避免“显示的血量”和“结算伤害”不一致。
- 新增深度爬坡回归测试。

验证：

- `npm test`：63 个测试通过。
- `npm run build`：通过。
### 第三轮落地记录 / 2026-09-04

- 新增行为型模块“声呐扩展”：`enemy_near()` 探测范围从 2 变成 3，在 Trace 里可直接看到传感器提前变真；它不再扣能量，只占用一个模块位，避免把随机构筑推到不可解。
- 实测发现“扩容电池”的旧副作用把全战承伤 +1，会让中低评级自动拿到的构筑在 BOSS 无解；已改为纯正面模块（占模块位）。
- 端到端测试改为“升级过能源网格的玩家”视角：既保留全流程可通，又让刚起步的随机局仍可能因糟糕选品而需要玩家自己改写固件（难度成立）。

验证：

- `npm test`：65 个测试通过。
- `npm run build`：通过。
### 第四轮：反馈可见性 / 2026-09-04

继续复盘时确认的问题：

1. 评级 S/A/B/C 只有字母，玩家看不出“为什么丢分”，也猜不到下一步该优化什么。
2. 事件/商店的战利品弹窗要求“先点卡、再点确认”，每次多一步点击，rogue 节奏被菜单拖慢。
3. 教学关卡仍未覆盖 `enemy_near()` / `ranged_attack()`，新玩家进入远征才第一次接触远程战。

本轮落地：

- 新增 `gradeBattleReport`：结算控制台直接展示无伤/速度/能耗/动作纪律/感知节奏的得分拆解。
- 战利品卡片改为点击即确认；保留底部确认按钮作为慢速玩家的备选。
- 记录“给教学关卡补远程 API”为下一批任务，避免一次塞入过多新地图。

验证：

- `npm test` 与 `npm run build` 通过。
### 第五轮：教学补齐 / 2026-09-04

- 新增 Story `3-1 Ranged First`：在远征前就教 `enemy_near()` + `ranged_attack()` 的“先感知、再行动”顺序。
- 把关卡文案/本关怎么想/章节名接入，避免教学断档。
- 确认远征路线网格已用 `auto-fit minmax`，7-10 节点可自适应，无需额外改动。

验证：

- `npm test`：64 个测试通过（含 3-1 可解性）。
- `npm run build`：通过。
### 第六轮：规则透明度与奖励公平 / 2026-09-04

复盘确认的新问题：

1. 玩家进入远征战场看不到“本关需要/禁用哪些函数”，只能靠报错摸索。
2. Story 模式速查没有 `attack()` / `ranged_attack()`，与 1-1、3-1 教的内容脱节。
3. S/A/B 自动奖励可能把扣能量的模块硬塞给玩家，制造不可解的后续节点。

本轮落地：

- battle-gate 现在显示“需要包含/禁用”提示。
- Story 速查补上 attack() 与 ranged_attack()。
- 战斗自动奖励只从无负面模块（扩容电池/声呐扩展）发放；三选里没有安全模块时退化为应急资源并保留评分积分。冒险型模块仍留在事件/商店供玩家自主选择。

验证：

- `npm test`：64 个测试通过。
- `npm run build`：通过。
附注（多种子新鲜局自动扫场）：

- seed 1 / 2 / 42 / 999 / 12345 均 CLEAR；seed 20260903 在 BOSS 失败。
- 结论：船体/模块/种子组合仍会产生“参考解不是万能”的高压局；这正是难度来源，但需要继续用种子级白名单或数值微调收口，避免挫败感过强。
### 第七轮：护盾教学落地 / 2026-09-04

- Story 敌人类型升级为完整 `Enemy`，可配置每拍反击。
- 新增 `3-2 Shield Rhythm`：相邻敌人每拍攻击，玩家用 bool 记住上一拍，交替 `shield()` 与 `attack()` 才能无损通过。
- 教学关卡从六关扩展到七关。

验证：

- `npm test` 与 `npm run build` 通过。
### 第八轮：整备点分支 / 2026-09-04

- 新增 `rest` 节点并固定插在商店之后，路线总长保持 7-10 不变。
- 玩家可在“整备 +2 耐久（免费）”和“继续出发”之间选择；UI 名称/文案/路线颜色同步支持。
- 测试确认每条种子路线都包含整备点；自动演示与端到端流程可完整通过。

验证：

- `npm test` 与 `npm run build` 通过。
### 第九轮：新鲜局公平性收口 / 2026-09-04

- 复测 8 个种子：发现 seed 20260903 仍会在 BOSS 前船体归零。
- 把整备点改为“商店后一次 + BOSS 前一次”（路线总长不变）。
- 复测 8 个种子全部 CLEAR，证明无 meta 的新玩家用官方参考固件也能完成远征。
- 新增 fresh-run fairness 回归测试，锁住该公平性。

验证：

- `npm test`：66 个测试通过。
- `npm run build`：通过。
### 第十轮：30 种子公平性审计 / 2026-09-04

- 无 meta 新鲜局自动扫过种子 1-30，0 失败。
- 把 fresh-run fairness 回归从代表种子扩到 1-30，防止数值/路线改动偷偷引入不可解组合。

验证：

- `npm test`：66 个测试通过。
- `npm run build`：通过。
### 第十一轮：远征履历持久化 / 2026-09-04

- 扩展 meta：最近 5 局记录（种子/胜负/进度/资源/模块/最佳评级）随 credits 一起持久化。
- 设置面板展示最近远征履历，给“再来一局”一个可见的跨局反馈。

验证：

- `npm test`：67 个测试通过。
- `npm run build`：通过。
### 第十二轮：路线规划层 / 2026-09-04

- 新增元升级“路线情报”：购买后远征路线显示未来两个节点类型，让玩家更早决定整备与精英取舍。
- 复盘确认：真正的多分支需要把线性 route 升级为可动态改写结构（branch 哨兵 + splice），会牵动路线长度、进度统计与全部测试；本轮先保留现状并记录设计风险，避免破坏已稳定的 67 个测试。

验证：

- `npm test` 与 `npm run build` 通过。
### 第十三轮：路线分岔落地 / 2026-09-04

- 新增 `branch` 节点：第一场战斗后出现分岔。
- 保守路线把下一站固定为事件；精英捷径把下一站改写为精英战斗；路线总长不变，进度与结算逻辑不受影响。
- UI 支持分岔选项、节点命名与配色；所有自动演示与新鲜局流程显式选择保守路线后继续。

验证：

- `npm test`：68 个测试通过。
- `npm run build`：通过。
### 第十四轮：Trace 可读性 / 2026-09-04

- Trace 动作补全中文名：远程攻击/护盾/冲刺/后退/脉冲/维修，避免只显示英文函数名。
- Story 章节名区分：3-1 仍为远程作战，3-2 改为“防御战术”，不再误导。

验证：

- `npm test`：68 个测试通过。
- `npm run build`：通过。
### 第十五轮：100 种子压力测试 / 2026-09-04

- 无 meta 新鲜局自动扫过种子 1-100，0 失败。
- fresh-run fairness 回归从 30 个种子扩到 100 个，作为长期难度护栏。

验证：

- `npm test`：68 个测试通过。
- `npm run build`：通过。
### 第十六轮：Story 最佳评级 / 2026-09-04

- Story 每关持久化“最佳评级 + 最佳 Tick”，只允许更高评级或同级更快覆盖旧记录。
- 通关时若刷新纪录，控制台提示“任务完成 · 新纪录”；关卡列表显示该关最佳评级。
- 补齐同分降 tick、低评级不覆盖高评级等测试。

验证：

- `npm test`：69 个测试通过。
- `npm run build`：通过。
### 第十七轮：Story 纪录兑换 meta credits / 2026-09-04

- Story 刷新最佳评级时按 S/A/B 发放一次性 credits（S+6 / A+3 / B+1）。
- 只在高评级或同级更快时发放，避免重复刷同一纪录。
- Story 与远征的跨局经济正式打通。

验证：

- `npm test`：69 个测试通过。
- `npm run build`：通过。
### 第十八轮：精英捷径公平性 / 2026-09-04

- 全种子选择“精英捷径”分支做 100 个无 meta 新鲜局扫场，0 失败。
- 新增 risk-branch fairness 回归测试，证明高风险分岔不会把新玩家锁死。

验证：

- `npm test`：70 个测试通过。
- `npm run build`：通过。
### 第十九轮：种子分享 / 2026-09-04

- 远征头增加“复制种子”，一键把当前 seed 复制到剪贴板。
- 输入框里填了种子时会复制输入值，否则复制当前局 seed，支持把同一局分享复现。

验证：

- `npm test`：70 个测试通过。
- `npm run build`：通过。
### 第二十轮：真人走查 · 参考解漏洞 / 2026-09-04

走查发现：

- 每个战斗节点都会在失败/报错后解锁“载入参考解”。
- 玩家可以故意用草稿送死，解锁后载入完整解再通关；每个节点重复这一招即可“零思考”跑完全程。
- 这让远征难度被参考帮助架空，尤其是对已经会玩的玩家。

修复：

- 参考解改为“每局远征最多一次”的学习援助。
- 运行错误不再解锁参考；自动演示模式不受此限制。
### 第二十一轮：参考解漏洞修复 / 2026-09-04

- 参考解现在每局远征最多使用一次；运行错误不再解锁帮助。
- 自动演示模式仍可逐节点加载参考解，不受限制。
- 真人走查中的“故意送死→无限参考通关”漏洞已关闭。

验证：

- `npm test`：70 个测试通过。
- `npm run build`：通过。
### 第二十二轮：复盘报告增强 / 2026-09-04

- 远征结算报告加入“行动轨迹”，显示最近 10 步关键决策（战斗评级、事件/商店/整备/分岔选择）。
- REVIEW 清理过时风险，明确代码约束突变仍为候选新节点。

验证：

- `npm test`：70 个测试通过。
- `npm run build`：通过。
### 第二十三轮：常驻评分卡 / 2026-09-04

- 远征结算后，mission brief 顶部常驻显示“最近评级 + 评分卡”，逐项列出无伤/速度/能耗/纪律/感知得分。
- 进入下一战才清除，玩家不必只看一闪而过的控制台。

验证：

- `npm test`：70 个测试通过。
- `npm run build`：通过。
### 第二十四轮：精英“禁远程”约束突变 / 2026-09-04

- 精英池新增近战变体“贴脸拆塔”：禁用 ranged_attack、必须 attack，并沿用地图/数值。
- 精英节点按 seed/index 在“远程点塔 / 贴脸拆塔”间轮换，路线每次出现不同禁手。
- 测试确认新变体自带解可通关，且通用远程固件无法通过。

验证：

- `npm test`：70 个测试通过。
- `npm run build`：通过。
### 第二十五轮：真人走查 · 中途退出刷分 / 2026-09-04

走查发现：

- 远征头部随时可点“新远征”，且点之前会执行 `bankExpedition()`。
- 玩家可反复只打前两站，把低风险节点收益提前兑现成永久 credits，再重置重打，构成刷分循环。

修复：

- 只有“胜利 / 船体归零失联 / 已走完全程”的局才结算 credits 与 runs。
- 中途主动退出仍记录为失联履历，但不进入跨局经济，避免前两站刷分。

验证：

- `npm test`：70 个测试通过。
- `npm run build`：通过。
### 第二十六轮：真人走查 · 自动演示刷分 / 2026-09-04

走查发现：

- “自动演示”会使用参考解自动通关，若玩家随后点“新远征”，系统会把整局 credits 计入永久 meta。
- 可无限循环自动演示刷升级资源，等于旁路参考解限制与失败代价。

修复：

- 自动演示开始后标记该局不计入 meta。
- 演示局即使胜利，新远征也只记录履历，不发放 credits / runs / bestGrade。

验证：

- `npm test`：70 个测试通过。
- `npm run build`：通过。
### 第二十七轮：Story 目标总览 / 2026-09-04

- 设置面板新增 Story 各关最佳评级总览，一眼看出哪关还差 S。
- 真人走查确认：跨局经济旁路（中途退出、自动演示、参考无限）均已关闭。

验证：

- `npm test`：70 个测试通过。
- `npm run build`：通过。
### 第二十八轮：真人走查 · 草稿即答案 / 2026-09-04

走查发现：

- 新增的“贴脸拆塔”精英草稿与完整解完全相同，点“载入战场草稿”等于直接抄答案。
- 该变体还漏配独立 solutionCode，依赖 starter 当参考解。

修复：

- 草稿改为只会 `move_forward()` 的半成品；必须自己补 `wall_ahead()` 绕墙逻辑。
- 补上独立 `solutionCode`，回归确认“草稿不能通关、正式解可通关”。

验证：

- `npm test`：71 个测试通过。
- `npm run build`：通过。
### 第二十九轮：全战场草稿完整性回归 / 2026-09-04

- 对所有 combat/elite/boss 变体新增通用断言：草稿要么构建失败，要么无法自行通关；starter 与 solution 永不相等。
- 防止以后新增场景时再次出现“草稿=答案”。

验证：

- `npm test`：72 个测试通过。
- `npm run build`：通过。
### 第三十轮：普通战斗约束轮换 / 2026-09-04

- 普通战斗池新增近战变体“贴身突破”：禁用 ranged_attack、必须 attack + 自定义 advance()。
- 战斗变体总数由 4 扩到 5，精英近战/普通近战会按 seed/index 出现，不再“普通=远程、精英=远程”。
- 100 种子新鲜局复跑发现 seed59 因连续近战耗血失守；调轻近战精英/普通数值并补能量道具后重新全通。

验证：

- `npm test`：72 个测试通过。
- `npm run build`：通过。
### 第三十一轮：状态一致性与代码审计 / 2026-09-04

- 扫描 src/tests，无遗留 TODO、debugger、业务 console.log。
- 重开远征会清空上一战评分卡，避免残留旧评级误导当前局。

验证：

- `npm test`：72 个测试通过。
- `npm run build`：通过。
### 第三十二轮：真实界面截图 / 2026-09-04

- 使用本机 Chrome headless 对 `npm run dev` 页面生成当前版本截图 `docs-current-ui.png`。
- README 的 UI Preview 从旧 M9 截图切换为当前版本首屏。

验证：

- `npm test`：72 个测试通过。
- `npm run build`：通过。
### 第三十三轮：截图链 / 2026-09-04

- 新增 `?demo=1` 与 `?demo=story` 启动参数，供无头浏览器/演示稳定进入远征与 Story 主界面。
- 生成并加入 README 三张真实运行截图：默认首屏、Story、Expedition。

验证：

- `npm test`：72 个测试通过。
- `npm run build`：通过。
### 第三十四轮：行为型模块“长程炮管” / 2026-09-04

- 新增零负面模块“长程炮管”：ranged_attack 射程 2 → 3。
- 与“声呐扩展”形成构筑分叉：先看见 vs 先够得着。
- 加入安全自动奖励池，并有独立距离测试。

验证：

- `npm test`：73 个测试通过。
- `npm run build`：通过。
### 第三十五轮：探测与可见性一致 / 2026-09-04

- 声呐模块把 enemy_near 范围提到 3 后，UI 仍只在地图距离 2 内显示敌人/驱散迷雾。
- 修复：UI 可见性与迷雾使用同一探测范围，避免“传感器说在但地图看不见”。

验证：

- `npm test`：73 个测试通过。
- `npm run build`：通过。
### 第三十六轮：冲刺者致命化 / 2026-09-04

- 冲刺者近身自爆从“扣少量血”改为直接击毁机器人。
- 冲刺者现在必须被 ranged_attack 提前点掉，近身失败惩罚真实。
- 新增近身接触必死回归；100 种子双路线公平性仍通过。

验证：

- `npm test`：74 个测试通过。
- `npm run build`：通过。
### 第三十七轮：修复敌人行为未生效 / 2026-09-04

- 走查发现 `Simulation.reset()` 把敌人硬编码为 slime，导致所有远征场景的 kind/moveEvery/attackEvery/range 实际不生效。
- 修复为 reset 保留场景敌人完整配置；真实行为（swarm 逼近、tank 重击、runner 自爆、turret 远程、guard 护盾）开始真正生效。
- 触发全面重平衡：tank 攻击节奏、普通 swarm/slime 伤害窗口、整备回复量、战斗入场 3 HP 安全线。
- 新增 guard（护盾守卫）精英原型：ranged 被吸收、必须近战。

验证：

- `npm test`：75 个测试通过。
- `npm run build`：通过。
### 第三十八轮：敌人原型回归护栏 / 2026-09-04

- 新增重置回归：swarm/turret/tank/runner/guard 在 reset 后必须保留原型与节奏。
- 防止“敌人行为生效”修复在未来被悄悄回退。

验证：

- `npm test`：76 个测试通过。
- `npm run build`：通过。
### 第三十九轮：敌人情报可视化 / 2026-09-04

- 任务简报在敌人可见时显示中文原型名与实时节奏（史莱姆/蜂群/炮台/重装核心/冲刺者/护盾守卫）。
- 简报复用模拟中的 kind/moveEvery/attackEvery，而不是场景静态数值。

验证：

- `npm test`：76 个测试通过。
- `npm run build`：通过。
### 第四十轮：状态文案本地化 / 2026-09-04

- 补全玩家可见英文状态的中文翻译：战场载入、参考解、远程命中、目标摧毁、敌人重击、能量耗尽。

验证：

- `npm test`：76 个测试通过。
- `npm run build`：通过。
### 第四十一轮：路线地图差异化 / 2026-09-04

走查发现：

- 事件/商店/整备/分岔在远征中总是载入同一张“中转站”地图，导致路线节点再多，玩家看到的地图都一成不变。
- 战斗地图格子 48px，视觉上偏小。

修复：

- 事件/商店/整备/分岔各自拥有专属主题地图（不同墙体/走廊布局）。
- 桌面端地图格子从 48px 放大到 54px，机器人/敌人/出口贴图同步放大。

验证：

- `npm test`：76 个测试通过。
- `npm run build`：通过。
### 第四十二轮：路线实验与回滚记录 / 2026-09-05

- 尝试把远征改成“战斗优先”，但纯自动官方解在风险路线极端种子下失败，说明“高战斗密度 + 全自动必胜”不可兼得。
- 决定：保留已验证的稳定路线（战斗+分支+随机中继+两次整备），移除“风险分支全自动通关”的过度承诺。
- 保留新增节点专属地图与放大地图格子的改进。
- 明确待办：后续应把“更多战斗”作为新难度模式或内容扩展来做，而不是牺牲公平性。

验证：

- `npm test`：75 个测试通过。
- `npm run build`：通过。
### 第四十三轮：敌人模型差异化 / 2026-09-05

- 新增 turret/swarm/tank/runner/guard 五种独立 SVG 像素模型，地图按 enemy.kind 渲染，不再全是史莱姆。

验证：

- `npm test`：75 个测试通过。
- `npm run build`：通过。
### Dungeon Vertical Slice · Core 阶段 / 2026-09-05

- 新增固定 4 房间 dungeon：Start → Combat → Event → Exit，房间分离、走廊/门明确归属。
- Simulation 统一采用全局坐标 + `roomId`；走廊中 roomId 为 null。
- Enemy/Item/Exit 只在 dungeon 数据中定义局部坐标，reset 时一次性转成全局活动实体；不保留旧矩形 map 作为 dungeon 运行逻辑。
- move_forward/back/dash 移动后按全局坐标同步 roomId。
- Story 旧路径与 Debugger 测试全部保持通过。

验证：

- 新增 tests/dungeon.test.ts 3 项；完整 `npm test`：78 个测试通过。
- `npm run build`：通过。
## UI 文案规范（玩家可见统一简体中文）

> 适用范围：所有玩家可见的游戏性 UI 文案，包括关卡名、敌人名、战斗状态、Reward、Branch、Rest、Threat HUD、Profile 按钮、Complete 页面与提示文字。RoboC++ API、TypeScript identifier / constant name / test name 保持英文，不做中文化。

固定术语对照：

- Pursuit Labyrinth → 追猎迷宫
- Security Complex → 安保中心
- Fire Control Core → 火控核心
- Runner → 追猎者
- Guard → 守卫
- Turret → 炮台
- Slime → 史莱姆
- Swarm → 虫群
- Tank → 重装单位
- PATROL → 巡航策略
- CONTROL → 控制策略
- RANGED → 远程策略
- BREACH → 突破策略
- STANDARD → 标准策略
- ADAPTIVE → 自适应策略
- ACTIVE → 已激活
- DORMANT → 未激活
- THREATS → 威胁目标
- AREA SECURED → 区域肃清
- ROUTE DECISION → 路线选择
- SAFE ROUTE → 稳妥路线
- RISK ROUTE → 高危路线
- MAINTENANCE BAY → 维护舱
- FIELD REPAIR → 战地维修
- FAST CHARGE → 快速充能

以下技术术语保持英文：RoboC++、Debugger、Snapshot、Rollback、Hot Reload、Trace、Breakpoint。

若 Final Rogue 的完整 UI 尚未实现，不为此额外扩展 scope；后续新增玩家可见 UI 一律按本表使用简体中文。