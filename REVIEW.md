# CodeRogue 客观复盘（新玩家 / 部分玩家视角）

> 生成方式：阅读 `src/core.ts`、`src/language.ts`、`src/App.vue` 与测试，运行 `npm run build` 和 `npm test`，并用“第一次玩的人 / 玩过一点的人”两种心智模型走查流程。未进行真实浏览器点击，UI 观感仍建议人工复核。

## 一、当前状态

- 构建：通过（Vite + tsc）。
- 测试：75 个测试全部通过。
- 分支：`dev`。

## 二、已确认并修复的问题

1. 新手引导结束后仍停留在远征模式，而不是进入第一关。修复为引导结束时切到教学关卡并加载示例。
2. 胜利后“开启新远征”使用 `location.reload()`，会丢掉刚累计的跨局资源。修复为调用 `newExpedition()`，先结算再重置。
3. 教学/远征模式切换时不会主动停止计时器，可能在后台继续跑。修复为切换前先 `stop()`。
4. 远程炮台仍显示“接触后每 X Tick 攻击”。修复为根据敌人类型显示“远程射击”。
5. 跨局元进度的“运行次数”在没有完成任何节点时也会 +1。修复为仅在 `nodesCleared > 0` 时计数。
6. 新传感器 `enemy_near()`、`low_energy()` 和新动作 `shield()` 未出现在语法速查。已补入战术速查。
7. 远征战斗胜利后仍需在“强攻/防御/漏洞利用”菜单里二次结算。修复为固件评分直接结算：S/A/B 自动授予对应模块，C 给应急资源，不再出现战术菜单。
8. 一份通用固件可以通吃大部分远征战场。修复为普通战斗/精英禁真实 attack、BOSS 禁真实 ranged_attack，并把约束匹配改成函数名边界。
9. “载入参考解”一键通关，正常游玩没有难度。修复为首次进入不显示，失败后解锁。
10. 失败零代价且设置里的机体升级不可购买。修复为损毁扣船体、船体归零失联，并补全元升级目录与购买持久化。

## 三、从新玩家视角看到的优点

- 第一关目标足够短，示例代码能直接构建运行，符合“先成功再改动”。
- Trace Replay 能把动作、传感器、变量、坐标对应回源码行，调试链路清晰。
- 教程用三步解释 `update()`、传感器/动作、首跑流程，中文界面降低了门槛。
- 远征种子现在可复现、可随机，避免每次都一样的重玩疲劳。

## 四、从部分玩家视角看到的剩余风险

1. 元进度已有路线情报、长程炮管、声呐扩展等行为变化；仍可继续增加“新传感器/动作”解锁层。
2. 新动作教学已覆盖 ranged_attack 与 shield（Story 3-1/3-2）；guard/runner/turret/tank/swarm 的真实行为已生效并有回归。
3. 无 meta 新玩家在保守与精英捷径两条路线上的 100 种子抽样均全通，已固化回归。
4. 事件/商店/整备/分岔仍以菜单决策表达；点击即选已提速，可继续做轻量化表达。
5. 路线含 combat/elite/event/shop/rest/branch/boss，7-10 节点且网格 auto-fit；可扩展更多分支节点。

## 五、建议的下一批工作

- 评级理由已写入结算控制台；下一步让“本战评分卡”常驻并逐项可见。
- 代码约束突变已以“精英禁远程/贴脸拆塔”变体落地；可继续扩展为普通战斗随机禁手。
- 复盘报告已显示种子/节点/资源/模块/最近行动轨迹；可继续扩展为按节点评分列表。