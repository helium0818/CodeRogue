# CodeRogue 长期规划与目标

> 本文档回答三个问题：这个游戏要成为什么、当前为什么不够好玩、以及接下来按什么顺序改。
> 它是路线图，不是验收清单；具体可执行任务会拆进 `TASKS.md`。

## 1. 愿景（North Star）

让玩家像调试真实程序一样思考：每一局随机远征，都要求玩家为“此时此地的敌人和地形”写出、验证、并迭代一套 RoboC++ 固件。代码质量直接决定生存、收益与构筑，失败会留下可复用的知识与蓝图。

三根支柱保持不变：

- 玩家写的是机器人的持久大脑，程序每拍只执行一个世界动作。
- 构建、运行、观察、追踪、调试、改进是核心循环。
- 编程门槛低，但深度没有上限：传感器、状态、数组、循环、函数都应该是通关手段，而不是摆设。

## 2. 现状诊断（为什么不够好玩）

基于对 `src/core.ts`、`src/language.ts`、`src/App.vue` 的阅读，当前主要问题是：

1. **远征是“假随机”**：`src/App.vue` 写死了 `new ExpeditionRun(20260903)`，每次路线、敌人、奖励都相同，重开没有新鲜感。
2. **代码没有成为胜负核心**：战斗结束后玩家在“强攻 / 防御 / 漏洞利用”之间做菜单选择，结果都是写死的数字；代码结构只通过 tick 数和掉血间接影响评级。
3. **示例代码能自动通关**：Story 五关都可以“载入示例 → 构建 → 运行”完成，没有激励玩家真正理解或改写算法。
4. **语言能力大于内容需求**：解释器已经支持 `for`、数组、用户函数、短路逻辑和类型检查，但没有关卡真正要求玩家使用它们。
5. **构筑很浅**：奖励池只有 4 个模块，且大多只是改数值（`attackPower`、`maxEnergy`、`moveEnergyCost`、`incomingDamage`），多数只在特定节点生效。
6. **敌人行为单一**：只有 `slime` 一种 `kind`，行为差异仅来自 `hp`、`moveEvery`、`attackEvery`。
7. **失败代价低**：机体损毁后 BOSS 关仍可“紧急撤离保留战利品”，缺少真实的风险与后果。
8. **运行中玩家主动性少**：除远征战斗的一次性脉冲干扰外，运行过程基本是观看。

已有的强项（要保留）：确定性 tick 引擎、逐帧 Trace Replay、断点/监视点/快照/回滚调试器、严格受限的语言运行时、清晰的数据驱动关卡结构、以及成熟的 IDE 工作台视觉。

## 3. 目标（Goals）

这些是可衡量的产品目标，后续每个阶段都要能回答“是否向目标靠近了”。

1. **每一局都不一样**：默认使用随机种子，同时支持输入种子复现和分享；路线长度、节点类型、敌人参数、奖励池至少四个维度产生变化。
2. **代码是胜负核心**：至少四类敌人原型，各自需要不同算法；战斗评级由 tick 数、受伤、能耗、传感器读取次数、动作数综合计算，并直接映射奖励。
3. **内容吃掉语言能力**：新增关卡和远征任务强制使用数组、`for` 或用户函数；新增少量传感器和动作。
4. **构筑可见且可成长**：模块不再只是改数值，而是改变模拟行为（远程攻击、护盾、能量回收、脉冲次数等），并能在世界状态和 Trace 中被观察。
5. **跨局元进度**：引入 credits / 蓝图 / 解锁，让“再来一局”有长期目标。
6. **失败有意义**：机体损毁有明确代价，高风险节点有真实回报；逃离不再是零成本的万用解法。
7. **保持可教学性**：新玩家仍能在 5 分钟内完成第一关，并理解 `update()`、传感器、动作的区别。

## 4. 非目标（Non-Goals）

- 不引入真实多线程、任意代码执行、本地文件访问或网络联机。
- 不把 RoboC++ 扩展成完整 C++；它仍然是刻意受限的教学语言。
- 不做 3D、复杂骨骼动画或大体量美术资产。
- 不牺牲确定性调试能力：任何运行都可通过种子复现，Trace 可回放。
- 不把“更多菜单”当作“更多玩法”；每个新系统都要服务于写代码或改代码。

## 5. 设计原则

- **代码优先**：如果一个问题可以用“写一段更聪明的固件”解决，就优先这么做，而不是加一个按钮或菜单。
- **可解释的失败**：玩家每次失败都应该能从 Trace、评分或提示中看出“代码哪里需要改”。
- **渐进披露**：新传感器、新语法、新敌人都要先用一个低压力关卡教会玩家，再进入高风险节点。
- **种子即真相**：随机性通过种子生成，既能带来变化，也能保证复现与调试。
- **行为型构筑优先于数值型构筑**：优先做“改变机器人能做什么”的模块，其次才是“把数字变大”。

## 6. 阶段路线图

### 阶段 0：对齐与基线（当前）

- 锁定本文档中的愿景、目标和非目标。
- 保留当前 `main` 基线提交，后续开发在 `dev` 分支进行。
- 验收：团队/玩家对“要成为什么、不做什么”有一致认知。

### 阶段 1：随机与运行循环

目标：先让“每一局都不一样”成立，这是重玩价值的基础。

- 默认随机种子，支持输入/复制种子；界面始终显示当前 seed。
- 路线生成器：长度 6–10，节点类型按权重生成，BOSS 固定在末端。
- 敌人与地图参数带受控随机修正（`hp`、`moveEvery`、`attackEvery`、地形扰动）。
- 奖励池随机化，保留重复模块的补偿规则。
- 明确单局状态机：开始 → 逐节点 → 结算 → 胜利 / 失败 / 撤离。
- 持久化最近运行记录（seed、结果、评分、路线摘要）。

验收：

- 连续 3 局路线、敌人、奖励不重复。
- 相同 seed 得到完全一致的结果，可复现、可回放。

### 阶段 2：让代码决定胜负

目标：把“结算菜单”替换为“代码表现决定结果”。

- 四类敌人原型：
  - 追击者：现有 slime 强化，逼近并接触攻击。
  - 远程炮台：不追但隔空攻击，要求“尽快接近或绕开”。
  - 护盾守卫：正面攻击无效，要求绕后或先破盾。
  - 加速/自爆者：需要快速处理，否则造成高额伤害。
- 每个敌人原型有明确的“反制算法”，关卡文案与战术卡提示，但不直接给答案。
- 战斗评分：`tick 数`、`受伤`、`能耗`、`传感器读取次数`、`动作数` 综合为 S/A/B/C，并映射到 credits 和蓝图。
- 弱化或移除独立的“战术结算”菜单；把玩家选择变为代码策略的自然延续。
- 远征非战斗节点改为小型“代码决策挑战”，而不是纯菜单。

验收：

- 不同敌人需要不同固件策略。
- 玩家能解释“为什么这个敌人需要这样写代码”。
- 评分与奖励之间的关系可被玩家理解。

### 阶段 3：语言与内容扩张

目标：让已经存在的语言能力成为通关手段。

- 新增少量传感器和动作，例如：
  - 传感器：`enemy_near()`（距离 ≤ 2）、`low_energy()`。
  - 动作：`shield()`（本拍/后续减伤）、`ranged_attack()`（消耗更多能量）。
- 新增 3 个以上关卡或远征任务，强制使用数组、`for` 或用户函数：
  - “记忆巡逻路径”需要数组记录方向。
  - “统计遇敌次数”需要 `int` 与循环。
  - “写一个 `turn_toward()` 辅助函数”需要用户函数。
- 引入“代码约束突变”：某些节点要求“不能使用 attack”“最多 N 行”“必须定义并调用一个函数”，让写作本身成为挑战。
- 扩展语法速查与“如何想”卡牌覆盖新特性。

验收：

- 新内容必须实际使用高级语言特性才能通过。
- 约束任务可玩、可验证，并且失败原因可解释。

### 阶段 4：元进度与构筑

目标：形成长期目标和可成长的构筑。

- 引入跨局货币 credits / 蓝图。
- 永久解锁：新传感器、新动作、新调试器能力、新章节。
- 构筑模块改为行为型：远程攻击、护盾、能量回收、脉冲次数 +1、移动不耗能等，直接改变模拟。
- 失败结算：机体损毁扣除耐久/资源，但保留已获蓝图；BOSS 撤离不再是零成本。
- 模块效果能在世界状态、Trace 和结算报告中观察到。

验收：

- 玩家有“再来一局以解锁 X”的动机。
- 每个模块都能在模拟中产生可见的行为变化，而不是只改变一个数字。

### 阶段 5：打磨与平衡

目标：把深度做成可被新老玩家都感知的体验。

- 平衡敌人数值、路线风险与奖励曲线。
- 覆盖新敌人、新传感器、新约束的新手引导。
- 单局复盘报告：路线、评分、模块、致命决策与改进建议。
- 音频、视觉、响应式布局与性能检查。
- 用户测试与数据驱动的难度调整。

验收：

- 新玩家 5 分钟内完成第一关并理解核心循环。
- 资深玩家能通过评分、难度和构筑感受到成长。
- 失败后玩家知道下一步该改哪行代码。

## 7. 成功指标

- 首次会话完成第一关的转化率 > 80%（通过用户测试观察）。
- 玩家主动分享种子或重开不同种子的行为出现。
- 深度关卡（使用数组 / 循环 / 函数）的完成率与回放率显著提升。
- 玩家能用自己的话解释至少一个敌人对应的代码策略。
- 失败后继续重试的比例提升，而不是退出。

## 8. 立即要做的三件事

1. 实现种子系统：默认随机、可输入复现、界面显示 seed，并替换 `App.vue` 中的固定种子。
2. 设计并落地第一类新敌人原型（远程炮台），用它验证“不同敌人需要不同代码”这条核心假设。
3. 用一个小型“代码约束突变”节点验证“写作本身就是挑战”是否成立，再决定是否扩大。
